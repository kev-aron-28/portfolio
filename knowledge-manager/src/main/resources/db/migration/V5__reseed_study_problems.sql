-- Auto-generated by scripts/generate_problem_seed.py
-- Re-seed study problems from personal notes

BEGIN;

-- 1) Delete old V4/V5 personal-note seed data
DELETE FROM reviews WHERE problem_id IN (SELECT id FROM problems WHERE description LIKE 'Study problem from personal notes%' OR description LIKE 'Imported from personal study notes.%');
DELETE FROM problem_tags WHERE problem_id IN (SELECT id FROM problems WHERE description LIKE 'Study problem from personal notes%' OR description LIKE 'Imported from personal study notes.%');
DELETE FROM solutions WHERE problem_id IN (SELECT id FROM problems WHERE description LIKE 'Study problem from personal notes%' OR description LIKE 'Imported from personal study notes.%');
DELETE FROM problems WHERE description LIKE 'Study problem from personal notes%' OR description LIKE 'Imported from personal study notes.%';

-- 2) Delete orphaned rows
DELETE FROM reviews r WHERE NOT EXISTS (SELECT 1 FROM problems p WHERE p.id = r.problem_id);
DELETE FROM problem_tags pt WHERE NOT EXISTS (SELECT 1 FROM problems p WHERE p.id = pt.problem_id);
DELETE FROM problem_tags pt WHERE NOT EXISTS (SELECT 1 FROM tags t WHERE t.id = pt.tag_id);
DELETE FROM solutions s WHERE NOT EXISTS (SELECT 1 FROM problems p WHERE p.id = s.problem_id);

-- 3) Delete unused tags and empty topics, including excluded legacy buckets
DELETE FROM tags t WHERE NOT EXISTS (SELECT 1 FROM problem_tags pt WHERE pt.tag_id = t.id);
DELETE FROM topics t
WHERE t.name IN ('Practice Exams', 'Daily Practice', 'Miscellaneous', 'Design')
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE p.topic_id = t.id);
DELETE FROM topics t
WHERE NOT EXISTS (SELECT 1 FROM problems p WHERE p.topic_id = t.id);

-- 4) Insert topics, tags, problems, empty solutions, and problem_tags
INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Arrays & Strings', 'Curated arrays & strings practice problems from personal notes.', '#3b82f6', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Backtracking', 'Curated backtracking practice problems from personal notes.', '#a855f7', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Binary Search', 'Curated binary search practice problems from personal notes.', '#6366f1', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Graphs', 'Curated graphs practice problems from personal notes.', '#ef4444', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Greedy', 'Curated greedy practice problems from personal notes.', '#eab308', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Hash Map / Set', 'Curated hash map / set practice problems from personal notes.', '#8b5cf6', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Linked List', 'Curated linked list practice problems from personal notes.', '#ec4899', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Prefix Sum', 'Curated prefix sum practice problems from personal notes.', '#14b8a6', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Sliding Window', 'Curated sliding window practice problems from personal notes.', '#06b6d4', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Sorting', 'Curated sorting practice problems from personal notes.', '#64748b', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Stack', 'Curated stack practice problems from personal notes.', '#f97316', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Trees', 'Curated trees practice problems from personal notes.', '#22c55e', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO topics (name, description, color, created_at, updated_at)
VALUES ('Two Pointers', 'Curated two pointers practice problems from personal notes.', '#f59e0b', NOW(), NOW())
ON CONFLICT (name) DO UPDATE
SET description = EXCLUDED.description,
    color = EXCLUDED.color,
    updated_at = NOW();

INSERT INTO tags (name, created_at)
VALUES ('Array', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('BFS', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Backtracking', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Binary Search', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('DFS', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Graph', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Greedy', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Hash Map', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Linked List', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Prefix Sum', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Set', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Sliding Window', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Sorting', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Stack', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('String', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Tree', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO tags (name, created_at)
VALUES ('Two Pointers', NOW())
ON CONFLICT (name) DO NOTHING;

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  '3Sum',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice 3sum using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('3Sum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = '3Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = '3Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = '3Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'All Paths From Source To Target',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice all paths from source to target using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('All Paths From Source To Target'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'All Paths From Source To Target'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'All Paths From Source To Target'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'All Paths From Source To Target'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Array Partition',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice array partition using core greedy patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Greedy'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Array Partition'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Array Partition'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Greedy'
WHERE p.title = 'Array Partition'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Array Partition'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'As Far from Land as Possible',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice as far from land as possible using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('As Far from Land as Possible'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'As Far from Land as Possible'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'As Far from Land as Possible'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'As Far from Land as Possible'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Balanced Binary Tree',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice balanced binary tree using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Balanced Binary Tree'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Balanced Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Balanced Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Balanced Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Beautiful Arrangement',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice beautiful arrangement using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Beautiful Arrangement'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Beautiful Arrangement'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Beautiful Arrangement'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Beautiful Arrangement'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Best Time to Buy and Sell Stock',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice best time to buy and sell stock using core arrays & strings patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Arrays & Strings'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Best Time to Buy and Sell Stock'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Best Time to Buy and Sell Stock'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Best Time to Buy and Sell Stock'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'String'
WHERE p.title = 'Best Time to Buy and Sell Stock'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Binary Search',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice binary search using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Binary Search'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Binary Search'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Binary Search'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Binary Search'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Binary Subarrays With Sum',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice binary subarrays with sum using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Binary Subarrays With Sum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Binary Subarrays With Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Binary Subarrays With Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Binary Subarrays With Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Binary Tree Inorder Traversal',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice binary tree inorder traversal using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Binary Tree Inorder Traversal'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Binary Tree Inorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Binary Tree Inorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Binary Tree Inorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Binary Tree Level Order Traversal',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice binary tree level order traversal using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Binary Tree Level Order Traversal'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Binary Tree Level Order Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Binary Tree Level Order Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Binary Tree Level Order Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Binary Tree Level Order Traversal II',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice binary tree level order traversal ii using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Binary Tree Level Order Traversal II'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Binary Tree Level Order Traversal II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Binary Tree Level Order Traversal II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Binary Tree Level Order Traversal II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Binary Tree Maximum Path Sum',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice binary tree maximum path sum using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Binary Tree Maximum Path Sum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Binary Tree Maximum Path Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Binary Tree Maximum Path Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Binary Tree Maximum Path Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Binary Tree Postorder Traversal',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice binary tree postorder traversal using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Binary Tree Postorder Traversal'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Binary Tree Postorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Binary Tree Postorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Binary Tree Postorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Binary Tree Preorder Traversal',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice binary tree preorder traversal using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Binary Tree Preorder Traversal'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Binary Tree Preorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Binary Tree Preorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Binary Tree Preorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Binary Tree Right Side View',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice binary tree right side view using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Binary Tree Right Side View'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Binary Tree Right Side View'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Binary Tree Right Side View'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Binary Tree Right Side View'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Binary Tree Zigzag Level Order Traversal',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice binary tree zigzag level order traversal using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Binary Tree Zigzag Level Order Traversal'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Binary Tree Zigzag Level Order Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Binary Tree Zigzag Level Order Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Binary Tree Zigzag Level Order Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Boats To Save People',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice boats to save people using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Boats To Save People'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Boats To Save People'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Boats To Save People'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Boats To Save People'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Bus Routes',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice bus routes using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Bus Routes'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Bus Routes'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Bus Routes'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Bus Routes'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Candy',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice candy using core greedy patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Greedy'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Candy'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Candy'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Greedy'
WHERE p.title = 'Candy'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Candy'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Capacity To Ship Packages Within D Days',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice capacity to ship packages within d days using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Capacity To Ship Packages Within D Days'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Capacity To Ship Packages Within D Days'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Capacity To Ship Packages Within D Days'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Capacity To Ship Packages Within D Days'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Car Fleet',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice car fleet using core sorting patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sorting'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Car Fleet'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Car Fleet'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sorting'
WHERE p.title = 'Car Fleet'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Car Fleet'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Climbingthe Leaderboard',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice climbingthe leaderboard using core greedy patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Greedy'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Climbingthe Leaderboard'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Climbingthe Leaderboard'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Greedy'
WHERE p.title = 'Climbingthe Leaderboard'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Climbingthe Leaderboard'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Clone Graph',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice clone graph using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Clone Graph'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Clone Graph'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Clone Graph'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Clone Graph'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Combination Sum',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice combination sum using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Combination Sum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Combination Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Combination Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Combination Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Combination Sum II',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice combination sum ii using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Combination Sum II'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Combination Sum II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Combination Sum II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Combination Sum II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Combination Sum III',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice combination sum iii using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Combination Sum III'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Combination Sum III'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Combination Sum III'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Combination Sum III'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Combinations',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice combinations using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Combinations'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Combinations'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Combinations'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Combinations'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Construct Binary Tree from Inorder and Postorder Traversal',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice construct binary tree from inorder and postorder traversal using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Construct Binary Tree from Inorder and Postorder Traversal'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Construct Binary Tree from Inorder and Postorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Construct Binary Tree from Inorder and Postorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Construct Binary Tree from Inorder and Postorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Construct Binary Tree from Preorder and Inorder Traversal',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice construct binary tree from preorder and inorder traversal using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Construct Binary Tree from Preorder and Inorder Traversal'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Construct Binary Tree from Preorder and Inorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Construct Binary Tree from Preorder and Inorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Construct Binary Tree from Preorder and Inorder Traversal'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Container With Most Water',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice container with most water using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Container With Most Water'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Container With Most Water'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Container With Most Water'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Container With Most Water'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Contains Duplicate',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice contains duplicate using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Contains Duplicate'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Contains Duplicate'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Contains Duplicate'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Contains Duplicate'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Contiguous Array',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice contiguous array using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Contiguous Array'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Contiguous Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Contiguous Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Contiguous Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Continuous Subarray Sum',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice continuous subarray sum using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Continuous Subarray Sum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Continuous Subarray Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Continuous Subarray Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Continuous Subarray Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Convert Binary Number In Linked List To Integer',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice convert binary number in linked list to integer using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Convert Binary Number In Linked List To Integer'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Convert Binary Number In Linked List To Integer'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Convert Binary Number In Linked List To Integer'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Count Complete Tree Nodes',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice count complete tree nodes using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Count Complete Tree Nodes'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Count Complete Tree Nodes'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Count Complete Tree Nodes'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Count Complete Tree Nodes'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Count Number Of Nice Sub Arrays',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice count number of nice sub arrays using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Count Number Of Nice Sub Arrays'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Count Number Of Nice Sub Arrays'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Count Number Of Nice Sub Arrays'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Count Number Of Nice Sub Arrays'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Count Of Range Sum',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice count of range sum using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Count Of Range Sum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Count Of Range Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Count Of Range Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Count Of Range Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Daily Temperatures',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice daily temperatures using core stack patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Stack'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Daily Temperatures'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Daily Temperatures'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Stack'
WHERE p.title = 'Daily Temperatures'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Defuse The Bomb',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice defuse the bomb using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Defuse The Bomb'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Defuse The Bomb'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Defuse The Bomb'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Defuse The Bomb'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Delete Node',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice delete node using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Delete Node'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Delete Node'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Delete Node'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Diameter Of Binary Tree',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice diameter of binary tree using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Diameter Of Binary Tree'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Diameter Of Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Diameter Of Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Diameter Of Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Diet Plan Performance',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice diet plan performance using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Diet Plan Performance'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Diet Plan Performance'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Diet Plan Performance'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Diet Plan Performance'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Evaluate Division',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice evaluate division using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Evaluate Division'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Evaluate Division'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Evaluate Division'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Evaluate Division'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Evaluate Reverse Polish Notation',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice evaluate reverse polish notation using core stack patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Stack'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Evaluate Reverse Polish Notation'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Evaluate Reverse Polish Notation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Stack'
WHERE p.title = 'Evaluate Reverse Polish Notation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Find All Anagrams In A String',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice find all anagrams in a string using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Find All Anagrams In A String'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Find All Anagrams In A String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Find All Anagrams In A String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Find All Anagrams In A String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Find First And Last Position Of Element',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice find first and last position of element using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Find First And Last Position Of Element'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Find First And Last Position Of Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Find First And Last Position Of Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Find First And Last Position Of Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Find In Mountain Array',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice find in mountain array using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Find In Mountain Array'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Find In Mountain Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Find In Mountain Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Find In Mountain Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Find Minimum In Rotated Sorted Array',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice find minimum in rotated sorted array using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Find Minimum In Rotated Sorted Array'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Find Minimum In Rotated Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Find Minimum In Rotated Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Find Minimum In Rotated Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Find Peak Element',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice find peak element using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Find Peak Element'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Find Peak Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Find Peak Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Find Peak Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Find Peak Element II',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice find peak element ii using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Find Peak Element II'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Find Peak Element II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Find Peak Element II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Find Peak Element II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Find Pivot Index',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice find pivot index using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Find Pivot Index'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Find Pivot Index'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Find Pivot Index'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Find Pivot Index'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Find Smallest Letter Greater Than Target',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice find smallest letter greater than target using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Find Smallest Letter Greater Than Target'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Find Smallest Letter Greater Than Target'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Find Smallest Letter Greater Than Target'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Find Smallest Letter Greater Than Target'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Find The Difference',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice find the difference using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Find The Difference'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Find The Difference'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Find The Difference'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Find The Difference'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Find The Town Judge',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice find the town judge using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Find The Town Judge'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Find The Town Judge'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Find The Town Judge'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Find The Town Judge'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'First Bad Version',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice first bad version using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('First Bad Version'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'First Bad Version'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'First Bad Version'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'First Bad Version'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'First Missing Positive',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice first missing positive using core greedy patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Greedy'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('First Missing Positive'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'First Missing Positive'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Greedy'
WHERE p.title = 'First Missing Positive'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'First Missing Positive'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'First Unique Character In A String',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice first unique character in a string using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('First Unique Character In A String'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'First Unique Character In A String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'First Unique Character In A String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'First Unique Character In A String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Fruit Into Baskets',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice fruit into baskets using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Fruit Into Baskets'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Fruit Into Baskets'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Fruit Into Baskets'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Fruit Into Baskets'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Generate Parentheses',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice generate parentheses using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Generate Parentheses'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Generate Parentheses'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Generate Parentheses'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Generate Parentheses'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Group Anagrams',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice group anagrams using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Group Anagrams'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Group Anagrams'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Group Anagrams'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Group Anagrams'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Grumpy Bookstore Owner',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice grumpy bookstore owner using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Grumpy Bookstore Owner'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Grumpy Bookstore Owner'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Grumpy Bookstore Owner'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Grumpy Bookstore Owner'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Guess Number Higher or Lower',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice guess number higher or lower using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Guess Number Higher or Lower'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Guess Number Higher or Lower'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Guess Number Higher or Lower'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Guess Number Higher or Lower'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Hand Of Straights',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice hand of straights using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Hand Of Straights'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Hand Of Straights'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Hand Of Straights'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Hand Of Straights'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Greedy'
WHERE p.title = 'Hand Of Straights'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Happy Number',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice happy number using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Happy Number'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Happy Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Happy Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Happy Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Height Checker',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice height checker using core sorting patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sorting'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Height Checker'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Height Checker'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sorting'
WHERE p.title = 'Height Checker'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Height Checker'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Insert Interval',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice insert interval using core sorting patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sorting'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Insert Interval'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Insert Interval'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sorting'
WHERE p.title = 'Insert Interval'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Insert Interval'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Intersection Of Two Arrays',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice intersection of two arrays using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Intersection Of Two Arrays'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Intersection Of Two Arrays'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Intersection Of Two Arrays'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Intersection Of Two Arrays'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Intersection of Two Linked Lists',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice intersection of two linked lists using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Intersection of Two Linked Lists'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Intersection of Two Linked Lists'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Intersection of Two Linked Lists'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Invert Binary Tree',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice invert binary tree using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Invert Binary Tree'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Invert Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Invert Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Invert Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Is Graph Bipartite',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice is graph bipartite using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Is Graph Bipartite'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Is Graph Bipartite'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Is Graph Bipartite'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Is Graph Bipartite'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Is Subsequence',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice is subsequence using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Is Subsequence'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Is Subsequence'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Is Subsequence'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Is Subsequence'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Keys And Rooms',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice keys and rooms using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Keys And Rooms'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Keys And Rooms'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Keys And Rooms'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Keys And Rooms'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Koko Eating Bananas',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice koko eating bananas using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Koko Eating Bananas'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Koko Eating Bananas'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Koko Eating Bananas'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Koko Eating Bananas'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Largest Rectangle In Histogram',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice largest rectangle in histogram using core stack patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Stack'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Largest Rectangle In Histogram'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Largest Rectangle In Histogram'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Stack'
WHERE p.title = 'Largest Rectangle In Histogram'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Left And Right Sum Differences',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice left and right sum differences using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Left And Right Sum Differences'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Left And Right Sum Differences'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Left And Right Sum Differences'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Left And Right Sum Differences'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Letter Case Permutation',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice letter case permutation using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Letter Case Permutation'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Letter Case Permutation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Letter Case Permutation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Letter Case Permutation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Letter Combinations Of A Phone Number',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice letter combinations of a phone number using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Letter Combinations Of A Phone Number'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Letter Combinations Of A Phone Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Letter Combinations Of A Phone Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Letter Combinations Of A Phone Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Letter Tiles Possibilities',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice letter tiles possibilities using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Letter Tiles Possibilities'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Letter Tiles Possibilities'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Letter Tiles Possibilities'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Letter Tiles Possibilities'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Linked List Cycle',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice linked list cycle using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Linked List Cycle'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Linked List Cycle'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Linked List Cycle'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Linked List Cycle II',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice linked list cycle ii using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Linked List Cycle II'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Linked List Cycle II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Linked List Cycle II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Longest Consecutive Sequence',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice longest consecutive sequence using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Longest Consecutive Sequence'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Longest Consecutive Sequence'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Longest Consecutive Sequence'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Longest Consecutive Sequence'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Longest Repeating Character Replacement',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice longest repeating character replacement using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Longest Repeating Character Replacement'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Longest Repeating Character Replacement'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Longest Repeating Character Replacement'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Longest Repeating Character Replacement'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Longest Substring With At Most K Distinct Characters',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice longest substring with at most k distinct characters using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Longest Substring With At Most K Distinct Characters'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Longest Substring With At Most K Distinct Characters'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Longest Substring With At Most K Distinct Characters'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Longest Substring With At Most K Distinct Characters'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Longest Substring Without Repeating Characters',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice longest substring without repeating characters using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Longest Substring Without Repeating Characters'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Longest Substring Without Repeating Characters'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Longest Substring Without Repeating Characters'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Longest Substring Without Repeating Characters'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Lowest Common Ancestor of a Binary Tree',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice lowest common ancestor of a binary tree using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Lowest Common Ancestor of a Binary Tree'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Lowest Common Ancestor of a Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Lowest Common Ancestor of a Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Lowest Common Ancestor of a Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Lowest Common Ancestor Of ABST',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice lowest common ancestor of abst using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Lowest Common Ancestor Of ABST'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Lowest Common Ancestor Of ABST'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Lowest Common Ancestor Of ABST'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Lowest Common Ancestor Of ABST'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Lowest Common Ancestor Of ABST'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Majority Element',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice majority element using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Majority Element'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Majority Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Majority Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Majority Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Max Consecutive Ones',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice max consecutive ones using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Max Consecutive Ones'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Max Consecutive Ones'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Max Consecutive Ones'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Max Consecutive Ones'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Maximum Average Subarray I',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice maximum average subarray i using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Maximum Average Subarray I'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Maximum Average Subarray I'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Maximum Average Subarray I'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Maximum Average Subarray I'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Maximum Depth of Binary Tree',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice maximum depth of binary tree using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Maximum Depth of Binary Tree'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Maximum Depth of Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Maximum Depth of Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Maximum Depth of Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Maximum Number Of Vowels In A String',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice maximum number of vowels in a string using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Maximum Number Of Vowels In A String'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Maximum Number Of Vowels In A String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Maximum Number Of Vowels In A String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Maximum Number Of Vowels In A String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Maximum Population Year',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice maximum population year using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Maximum Population Year'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Maximum Population Year'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Maximum Population Year'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Maximum Population Year'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Maximum Size Sub Array Sum Equals K',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice maximum size sub array sum equals k using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Maximum Size Sub Array Sum Equals K'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Maximum Size Sub Array Sum Equals K'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Maximum Size Sub Array Sum Equals K'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Maximum Size Sub Array Sum Equals K'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Maximum Subarray',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice maximum subarray using core arrays & strings patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Arrays & Strings'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Maximum Subarray'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Maximum Subarray'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Maximum Subarray'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'String'
WHERE p.title = 'Maximum Subarray'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Maximum Sum Subarray of Size K',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice maximum sum subarray of size k using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Maximum Sum Subarray of Size K'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Maximum Sum Subarray of Size K'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Maximum Sum Subarray of Size K'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Maximum Sum Subarray of Size K'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Median Of Two Sorted Arrays',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice median of two sorted arrays using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Median Of Two Sorted Arrays'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Median Of Two Sorted Arrays'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Median Of Two Sorted Arrays'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Median Of Two Sorted Arrays'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Meeting Rooms',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice meeting rooms using core sorting patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sorting'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Meeting Rooms'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Meeting Rooms'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sorting'
WHERE p.title = 'Meeting Rooms'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Meeting Rooms'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Merge Intervals',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice merge intervals using core sorting patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sorting'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Merge Intervals'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Merge Intervals'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sorting'
WHERE p.title = 'Merge Intervals'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Merge Intervals'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Merge Sorted Array',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice merge sorted array using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Merge Sorted Array'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Merge Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Merge Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Merge Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Merge Two Sorted Lists',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice merge two sorted lists using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Merge Two Sorted Lists'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Merge Two Sorted Lists'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Merge Two Sorted Lists'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Middle Of The Linked List',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice middle of the linked list using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Middle Of The Linked List'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Middle Of The Linked List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Middle Of The Linked List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Min Stack',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice min stack using core stack patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Stack'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Min Stack'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Min Stack'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Stack'
WHERE p.title = 'Min Stack'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Minimum Deletions To Make Character Frequencies Unique',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice minimum deletions to make character frequencies unique using core arrays & strings patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Arrays & Strings'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Minimum Deletions To Make Character Frequencies Unique'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Minimum Deletions To Make Character Frequencies Unique'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Minimum Deletions To Make Character Frequencies Unique'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'String'
WHERE p.title = 'Minimum Deletions To Make Character Frequencies Unique'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Minimum Depth Of Binary Tree',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice minimum depth of binary tree using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Minimum Depth Of Binary Tree'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Minimum Depth Of Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Minimum Depth Of Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Minimum Depth Of Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Minimum Difference Between Highestand Lowestof K Scores',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice minimum difference between highestand lowestof k scores using core sorting patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sorting'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Minimum Difference Between Highestand Lowestof K Scores'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Minimum Difference Between Highestand Lowestof K Scores'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sorting'
WHERE p.title = 'Minimum Difference Between Highestand Lowestof K Scores'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Minimum Difference Between Highestand Lowestof K Scores'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Minimum Genetic Mutation',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice minimum genetic mutation using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Minimum Genetic Mutation'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Minimum Genetic Mutation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Minimum Genetic Mutation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Minimum Genetic Mutation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Minimum Number Of Days To Make M Bouquets',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice minimum number of days to make m bouquets using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Minimum Number Of Days To Make M Bouquets'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Minimum Number Of Days To Make M Bouquets'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Minimum Number Of Days To Make M Bouquets'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Minimum Number Of Days To Make M Bouquets'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Minimum Size Subarray Sum',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice minimum size subarray sum using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Minimum Size Subarray Sum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Minimum Size Subarray Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Minimum Size Subarray Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Minimum Size Subarray Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Minimum Window Substring',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice minimum window substring using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Minimum Window Substring'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Minimum Window Substring'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Minimum Window Substring'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Minimum Window Substring'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Missing Number',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice missing number using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Missing Number'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Missing Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Missing Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Missing Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Move Zeroes',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice move zeroes using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Move Zeroes'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Move Zeroes'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Move Zeroes'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Move Zeroes'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'N Queens',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice n queens using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('N Queens'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'N Queens'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'N Queens'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'N Queens'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Nearest Exit From Entrance In Maze',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice nearest exit from entrance in maze using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Nearest Exit From Entrance In Maze'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Nearest Exit From Entrance In Maze'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Nearest Exit From Entrance In Maze'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Nearest Exit From Entrance In Maze'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Next Permutation',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice next permutation using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Next Permutation'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Next Permutation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Next Permutation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Next Permutation'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Non-overlapping Intervals',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice non-overlapping intervals using core greedy patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Greedy'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Non-overlapping Intervals'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Non-overlapping Intervals'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Greedy'
WHERE p.title = 'Non-overlapping Intervals'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Non-overlapping Intervals'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Number Of Islands',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice number of islands using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Number Of Islands'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Number Of Islands'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Number Of Islands'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Number Of Islands'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Open The Lock',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice open the lock using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Open The Lock'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Open The Lock'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Open The Lock'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Open The Lock'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Palindrome Linked List',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice palindrome linked list using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Palindrome Linked List'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Palindrome Linked List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Palindrome Linked List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Palindrome Partition',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice palindrome partition using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Palindrome Partition'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Palindrome Partition'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Palindrome Partition'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Palindrome Partition'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Partition List',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice partition list using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Partition List'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Partition List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Partition List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Path Sum',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice path sum using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Path Sum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Path Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Path Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Path Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Path Sum II',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice path sum ii using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Path Sum II'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Path Sum II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Path Sum II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Path Sum II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Path Sum III',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice path sum iii using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Path Sum III'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Path Sum III'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Path Sum III'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Path Sum III'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Peak Index in a Mountain Array',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice peak index in a mountain array using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Peak Index in a Mountain Array'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Peak Index in a Mountain Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Peak Index in a Mountain Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Peak Index in a Mountain Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Permutation In String',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice permutation in string using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Permutation In String'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Permutation In String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Permutation In String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Permutation In String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Permutation Sequence',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice permutation sequence using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Permutation Sequence'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Permutation Sequence'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Permutation Sequence'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Permutation Sequence'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Permutation String',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice permutation string using core arrays & strings patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Arrays & Strings'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Permutation String'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Permutation String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Permutation String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'String'
WHERE p.title = 'Permutation String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Permutations',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice permutations using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Permutations'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Permutations'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Permutations'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Permutations'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Permutations II',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice permutations ii using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Permutations II'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Permutations II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Permutations II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Permutations II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Product of Array Except Self',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice product of array except self using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Product of Array Except Self'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Product of Array Except Self'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Product of Array Except Self'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Product of Array Except Self'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Range Sum Query - Immutable',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice range sum query - immutable using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Range Sum Query - Immutable'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Range Sum Query - Immutable'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Range Sum Query - Immutable'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Range Sum Query - Immutable'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Ransom Note',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice ransom note using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Ransom Note'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Ransom Note'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Ransom Note'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Ransom Note'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Relative Sort Array',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice relative sort array using core sorting patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sorting'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Relative Sort Array'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Relative Sort Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sorting'
WHERE p.title = 'Relative Sort Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Relative Sort Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Remove Duplicates From Linked List',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice remove duplicates from linked list using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Remove Duplicates From Linked List'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Remove Duplicates From Linked List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Remove Duplicates From Linked List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Remove Duplicates From Sorted Array',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice remove duplicates from sorted array using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Remove Duplicates From Sorted Array'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Remove Duplicates From Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Remove Duplicates From Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Remove Duplicates From Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Remove Duplicates from Sorted List II',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice remove duplicates from sorted list ii using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Remove Duplicates from Sorted List II'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Remove Duplicates from Sorted List II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Remove Duplicates from Sorted List II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Remove Element',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice remove element using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Remove Element'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Remove Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Remove Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Remove Element'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Remove Linked List Elements',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice remove linked list elements using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Remove Linked List Elements'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Remove Linked List Elements'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Remove Linked List Elements'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Remove Middle Node Of A Linked List',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice remove middle node of a linked list using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Remove Middle Node Of A Linked List'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Remove Middle Node Of A Linked List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Remove Middle Node Of A Linked List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Remove Nth Node From End of List',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice remove nth node from end of list using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Remove Nth Node From End of List'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Remove Nth Node From End of List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Remove Nth Node From End of List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Reorder List',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice reorder list using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Reorder List'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Reorder List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Reorder List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Reorder Routes To Make All Paths Lead To Zero',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice reorder routes to make all paths lead to zero using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Reorder Routes To Make All Paths Lead To Zero'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Reorder Routes To Make All Paths Lead To Zero'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Reorder Routes To Make All Paths Lead To Zero'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Reorder Routes To Make All Paths Lead To Zero'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Reorganize String',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice reorganize string using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Reorganize String'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Reorganize String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Reorganize String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Reorganize String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Replace Substring With Balanced String',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice replace substring with balanced string using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Replace Substring With Balanced String'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Replace Substring With Balanced String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Replace Substring With Balanced String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Replace Substring With Balanced String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Restore Ip Addresses',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice restore ip addresses using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Restore Ip Addresses'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Restore Ip Addresses'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Restore Ip Addresses'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Restore Ip Addresses'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Reverse Linked List',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice reverse linked list using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Reverse Linked List'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Reverse Linked List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Reverse Linked List'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Reverse String',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice reverse string using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Reverse String'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Reverse String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Reverse String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Reverse String'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Rotting Oranges',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice rotting oranges using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Rotting Oranges'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Rotting Oranges'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Rotting Oranges'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Rotting Oranges'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Running Sum Of 1 D Array',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice running sum of 1 d array using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Running Sum Of 1 D Array'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Running Sum Of 1 D Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Running Sum Of 1 D Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Running Sum Of 1 D Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Same Tree',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice same tree using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Same Tree'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Same Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Same Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Same Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Search in Rotated Sorted Array',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice search in rotated sorted array using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Search in Rotated Sorted Array'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Search in Rotated Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Search in Rotated Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Search in Rotated Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Search in Rotated Sorted Array II',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice search in rotated sorted array ii using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Search in Rotated Sorted Array II'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Search in Rotated Sorted Array II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Search in Rotated Sorted Array II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Search in Rotated Sorted Array II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Search Insert Position',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice search insert position using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Search Insert Position'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Search Insert Position'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Search Insert Position'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Search Insert Position'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Serializeand Deserialize Binary Tree',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice serializeand deserialize binary tree using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Serializeand Deserialize Binary Tree'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Serializeand Deserialize Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Serializeand Deserialize Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Serializeand Deserialize Binary Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Shortest Palindrome',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice shortest palindrome using core arrays & strings patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Arrays & Strings'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Shortest Palindrome'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Shortest Palindrome'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Shortest Palindrome'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'String'
WHERE p.title = 'Shortest Palindrome'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Shortest Path In Binary Matrix',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice shortest path in binary matrix using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Shortest Path In Binary Matrix'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Shortest Path In Binary Matrix'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Shortest Path In Binary Matrix'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Shortest Path In Binary Matrix'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Single Number',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice single number using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Single Number'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Single Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Single Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Single Number'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Sliding Window Maximum',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice sliding window maximum using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Sliding Window Maximum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Sliding Window Maximum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Sliding Window Maximum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Sliding Window Maximum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Smallest String From Leaf',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice smallest string from leaf using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Smallest String From Leaf'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Smallest String From Leaf'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Smallest String From Leaf'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Smallest String From Leaf'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Sort Characters By Frequency',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice sort characters by frequency using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Sort Characters By Frequency'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Sort Characters By Frequency'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Sort Characters By Frequency'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Sort Characters By Frequency'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Sort Colors',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice sort colors using core sorting patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sorting'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Sort Colors'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Sort Colors'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sorting'
WHERE p.title = 'Sort Colors'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Sort Colors'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Split Array Largest Sum',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice split array largest sum using core binary search patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Binary Search'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Split Array Largest Sum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Split Array Largest Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Split Array Largest Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Split Array Largest Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Split String Into Max Unique Substrings',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice split string into max unique substrings using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Split String Into Max Unique Substrings'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Split String Into Max Unique Substrings'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Split String Into Max Unique Substrings'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Split String Into Max Unique Substrings'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Squares of a Sorted Array',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice squares of a sorted array using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Squares of a Sorted Array'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Squares of a Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Squares of a Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Squares of a Sorted Array'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Subarray Sum Equals K',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice subarray sum equals k using core prefix sum patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Prefix Sum'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Subarray Sum Equals K'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Subarray Sum Equals K'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Prefix Sum'
WHERE p.title = 'Subarray Sum Equals K'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Subarray Sum Equals K'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Subsets',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice subsets using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Subsets'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Subsets'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Subsets'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Subsets'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Subsets II',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice subsets ii using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Subsets II'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Subsets II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Subsets II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Subsets II'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Substring with Concatenation of All Words',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice substring with concatenation of all words using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Substring with Concatenation of All Words'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Substring with Concatenation of All Words'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Substring with Concatenation of All Words'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Substring with Concatenation of All Words'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Substrings Of Size K With K Distinct Characters',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice substrings of size k with k distinct characters using core sliding window patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Sliding Window'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Substrings Of Size K With K Distinct Characters'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Substrings Of Size K With K Distinct Characters'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Sliding Window'
WHERE p.title = 'Substrings Of Size K With K Distinct Characters'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Substrings Of Size K With K Distinct Characters'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Sum Root To Leaf Numbers',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice sum root to leaf numbers using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Sum Root To Leaf Numbers'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Sum Root To Leaf Numbers'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Sum Root To Leaf Numbers'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Sum Root To Leaf Numbers'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Swap Nodes In Pairs',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice swap nodes in pairs using core linked list patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Linked List'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Swap Nodes In Pairs'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Swap Nodes In Pairs'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Linked List'
WHERE p.title = 'Swap Nodes In Pairs'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Symmetric Tree',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice symmetric tree using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Symmetric Tree'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Symmetric Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Symmetric Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Symmetric Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Task Scheduler',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice task scheduler using core greedy patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Greedy'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Task Scheduler'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Task Scheduler'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Greedy'
WHERE p.title = 'Task Scheduler'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Task Scheduler'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Top K Frequent Elements',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice top k frequent elements using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Top K Frequent Elements'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Top K Frequent Elements'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Top K Frequent Elements'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Top K Frequent Elements'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Trapping Rain Water',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice trapping rain water using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Trapping Rain Water'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Trapping Rain Water'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Trapping Rain Water'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Trapping Rain Water'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Two Sum',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice two sum using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Two Sum'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Two Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Two Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Two Sum'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Two Sum II - Input Array Is Sorted',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice two sum ii - input array is sorted using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Two Sum II - Input Array Is Sorted'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Two Sum II - Input Array Is Sorted'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Two Sum II - Input Array Is Sorted'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Two Sum II - Input Array Is Sorted'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Valid Anagram',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice valid anagram using core hash map / set patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Hash Map / Set'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Valid Anagram'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Valid Anagram'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Hash Map'
WHERE p.title = 'Valid Anagram'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Set'
WHERE p.title = 'Valid Anagram'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Valid Palindrome',
  NULL,
  'EASY',
  'Imported from personal study notes. Practice valid palindrome using core two pointers patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Two Pointers'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Valid Palindrome'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Valid Palindrome'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Two Pointers'
WHERE p.title = 'Valid Palindrome'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Valid Palindrome'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Valid Parentheses',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice valid parentheses using core stack patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Stack'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Valid Parentheses'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Valid Parentheses'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Stack'
WHERE p.title = 'Valid Parentheses'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Valid Sudoku',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice valid sudoku using core arrays & strings patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Arrays & Strings'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Valid Sudoku'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Valid Sudoku'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Array'
WHERE p.title = 'Valid Sudoku'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'String'
WHERE p.title = 'Valid Sudoku'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Validate Binary Search Tree',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice validate binary search tree using core trees patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Trees'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Validate Binary Search Tree'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Validate Binary Search Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Tree'
WHERE p.title = 'Validate Binary Search Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Validate Binary Search Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Binary Search'
WHERE p.title = 'Validate Binary Search Tree'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Word Ladder',
  NULL,
  'MEDIUM',
  'Imported from personal study notes. Practice word ladder using core graphs patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Graphs'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Word Ladder'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Word Ladder'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Graph'
WHERE p.title = 'Word Ladder'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'BFS'
WHERE p.title = 'Word Ladder'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problems (title, url, difficulty, description, favorite, archived, topic_id, created_at, updated_at)
SELECT
  'Word Search',
  NULL,
  'HARD',
  'Imported from personal study notes. Practice word search using core backtracking patterns.',
  FALSE,
  FALSE,
  t.id,
  NOW(),
  NOW()
FROM topics t
WHERE t.name = 'Backtracking'
  AND NOT EXISTS (SELECT 1 FROM problems p WHERE LOWER(p.title) = LOWER('Word Search'));

INSERT INTO solutions (problem_id, language, source_code, explanation, complexity, mistakes)
SELECT p.id, 'java', '', NULL, NULL, NULL
FROM problems p
WHERE p.title = 'Word Search'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'Backtracking'
WHERE p.title = 'Word Search'
  AND p.description LIKE 'Imported from personal study notes.%';

INSERT INTO problem_tags (problem_id, tag_id)
SELECT p.id, tg.id
FROM problems p
JOIN tags tg ON tg.name = 'DFS'
WHERE p.title = 'Word Search'
  AND p.description LIKE 'Imported from personal study notes.%';

COMMIT;
