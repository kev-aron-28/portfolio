# Terraform Projects

Two separate AWS labs. Each folder is its own Terraform root.

| Folder | What it provisions |
| --- | --- |
| `static-website-infra/` | S3 + CloudFront + ACM validation |
| `iam-user-managment/` | IAM users, login profiles, and roles |
